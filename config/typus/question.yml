# Use the README file as a reference to customize settings.

Question:
  fields:
    default: to_label
    form: 
  relationships: test_testquestions, tests, testquestionchoises
  application: Application
