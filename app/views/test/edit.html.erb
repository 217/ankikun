<%= form_for @test, :url => {:action => "create"} do |f| %>
	<%= f.text_area :body %>
<% end %>
<% @test1 = Test.new %>
<%= form_for @test1, :url => {:action => "create"} do |f| %>
	<%= f.text_area :body %>
<% end %>
