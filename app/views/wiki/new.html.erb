<h1>Wikiを新規作成</h1>
<%= form_for @wiki , :url => "/wiki/#{params[:id]}/create" do |form| %>
	<p>ページタイトル : <%= form.text_field :title %></p><br>
	<p>本文 : <br><%= form.text_area :body %></p><br>
	<%= form.submit "送信",:disable_with => "送信中" %>
<% end %>
